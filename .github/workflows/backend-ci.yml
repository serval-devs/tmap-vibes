name: Python CI (Windows)

on:
  push:
    branches:
      - main
      - Developing
      
  pull_request:
    branches:
      - main
      - Developing
      - feature/**
    paths:
      - .github/workflows/backend-ci.yml
      - Backend/**

jobs:
  build:
    runs-on: windows-latest

    strategy:
      matrix:
        python-version: ["3.10", "3.11"]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt
        shell: cmd
        working-directory: ./Backend

      - name: Check if model files exist
        run: |
          echo "Checking model and tokenizer files..."
          ls -l ./TensorflowModel/MLModels/
          ls -l ./TensorflowModel/MLModels/FakeTrueModel.keras
          ls -l ./TensorflowModel/MLModels/tokenizer.pkl

      - name: Run Linter (flake8)
        run: |
          flake8 .
        shell: cmd
        working-directory: ./Backend

      - name: Run Tests (pytest)
        run: |
          pytest --maxfail=1 --disable-warnings -q
        shell: cmd
        working-directory: ./Backend
